<%= linkto transition_paty(notification) do %>
  <div>
    <%= link_to notification.subject.customer.name, customer_path(notification.post.customer) %>
    があなたの
    <%= link_to '投稿', post_path(notification.subject.post) %>
    にいいねしました
    <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
<% end %>