<h4><%= @customer.name %>のフォロワー一覧</h4>
<% if @customers.exists? %>
  <% @customers.each do |a| %>
    <table>
      <tr>
        <th>ユーザー名</th>
        <th><%= link_to a.name, customer_path(a) %></th>
      </tr>
    </table>
  <% end %>
<% else %>
  <p>ユーザーはいません</p>
<% end %>